import { fetchAll } from './dump/fetch-all.dump';
import { fetchPaginate } from './dump/fetch-paginate.dump';

const dump = {
  fetchAll: fetchAll,
  fetchPaginate: fetchPaginate,
};

export default dump;